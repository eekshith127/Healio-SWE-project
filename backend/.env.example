# Backend Server Configuration
PORT=4000
HOST=0.0.0.0

# Firebase Admin SDK Configuration (REQUIRED)
# Download from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_PATH=./src/config/serviceAccountKey.json

# JWT Secret for Token Generation
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
NODE_ENV=development

# ============================================
# FIRESTORE DATABASE SETUP
# ============================================
# This app now uses Firebase Firestore (cloud database)
# NO LOCAL DATABASE INSTALLATION NEEDED!
#
# What you need:
# 1. Firebase project (already created: healio-bf5ed)
# 2. Service account key file (see FIREBASE_SERVICE_ACCOUNT_PATH above)
#
# Firestore collections created automatically:
# - users
# - bookings
# - notifications
# - medicines
#
# ============================================
# SETUP STEPS:
# ============================================
# 1. Copy this file: cp .env.example .env (Windows: copy .env.example .env)
# 2. Get Firebase service account key:
#    - Go to https://console.firebase.google.com/
#    - Select project: healio-bf5ed
#    - Settings > Service Accounts > Generate New Private Key
#    - Save as: backend/src/config/serviceAccountKey.json
# 3. Generate JWT secret:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Start server: npm start
#
# That's it! No MongoDB installation needed!
